const initialState = {
    error: false
};

const ContactReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
        return {...state}
    }
};

export { ContactReducer }
